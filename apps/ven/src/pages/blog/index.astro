---
import { getCollection } from 'astro:content';
import { BlogList } from '@prooq/ui';
import BaseLayout from '../../layouts/BaseLayout.astro';

const posts = (await getCollection('blog', ({ data }) => !data.draft))
  .sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf())
  .map((p) => ({
    slug: p.id,
    title: p.data.title,
    description: p.data.description,
    pubDate: p.data.pubDate,
    tags: p.data.tags,
  }));
---

<BaseLayout
  title="Blog | PROOQ — Tecnología y automatización para tu negocio en Venezuela"
  description="Artículos prácticos sobre digitalización, automatización, seguridad electrónica y tecnología para negocios en Venezuela."
>
  <BlogList posts={posts} locale="es-VE" />
</BaseLayout>
